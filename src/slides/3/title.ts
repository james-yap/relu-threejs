import { createHtmlPlane } from '../../utils';

export const slide3Title = createHtmlPlane({
  html: '$$y_i = x_iw + b$$',
  id: 'slide3Title',
  className: 'blue-text',
  width: 8,
  height: 1.5,
});

slide3Title.position.set(12.03, 4, 0.00);
slide3Title.position.z = 0.05;
