import { createHtmlPlane } from '../../utils';

export const slide2XLabel = createHtmlPlane({
  html: 'Study time (hours)',
  id: 'slide2XLabel',
  className: 'blue-text',
  width: 4,
  height: 0.8,
});

slide2XLabel.position.set(4.78, 0.57, 0.00);
